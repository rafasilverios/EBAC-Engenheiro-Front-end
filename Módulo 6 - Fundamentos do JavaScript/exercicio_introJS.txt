let base = prompt ("Digite o valor da base:");
let altura = prompt("Digite o valor da altura:");

function calculaArea(base, altura) {
  return base * altura;
}

alert("A área do objeto é: " + calculaArea(base, altura))